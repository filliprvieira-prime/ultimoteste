export interface Amenity {
  title: string;
  icon: string;
  description?: string;
}

export interface LocationTime {
  place: string;
  time: string;
}

export interface InfraItem {
  title: string;
  icon: string;
}